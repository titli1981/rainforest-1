<h1>Products</h1>

<%= link_to "New Product", new_product_path %>

<% @products.each do |product| %>
  <p> Name:<%= link_to (product.name), product_path(product)%>, Description:<%= product.description %>,Price:<%= product.price_in_cents %></p>
<% end %>
